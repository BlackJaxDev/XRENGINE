# OpenGL BackendObjects

Owns OpenGL API wrappers around engine resources such as buffers, textures, framebuffers, materials, mesh renderers, programs, queries, and samplers. Small enums belong in `Types/`.
