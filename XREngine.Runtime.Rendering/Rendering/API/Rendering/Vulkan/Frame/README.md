# Vulkan Frame

Owns explicit Vulkan frame lifecycle: swapchain acquire/recreate, synchronization objects, frame timing, resource retirement, and the top-level render loop. Command recording details stay under `Commands/`.
