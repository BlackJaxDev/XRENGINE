namespace XREngine.Rendering;

/// <summary>
/// Selects the texture channels exposed through a tooling preview handle.
/// </summary>
public enum RenderTexturePreviewChannel
{
    Rgba,
    Red,
    Green,
    Blue,
    Alpha,
    Luminance,
}
