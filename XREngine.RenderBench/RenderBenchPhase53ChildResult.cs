namespace XREngine.RenderBench;

/// <summary>Process and evidence identity for one isolated Phase 5.3 child.</summary>
internal readonly record struct RenderBenchPhase53ChildResult(
    string ResultPath,
    int ExitCode);
