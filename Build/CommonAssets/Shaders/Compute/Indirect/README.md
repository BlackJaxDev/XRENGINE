# Compute Indirect

Contains compute shaders that build and maintain indirect draw command data.

Typical responsibilities include counter reset, key generation, batch/command construction, and count/copy stages.
