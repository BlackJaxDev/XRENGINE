# Compute Sorting

Contains compute shaders responsible for ordering and key-based sorting in GPU-driven pipelines.

Typical responsibilities include radix/key index sorting stages used before indirect submission.
