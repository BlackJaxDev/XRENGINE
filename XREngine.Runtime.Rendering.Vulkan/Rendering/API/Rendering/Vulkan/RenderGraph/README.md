# Vulkan RenderGraph

Owns Vulkan-specific render-graph compilation, barrier planning, resource planning signatures, active-pass filtering, and graph/resource refresh policy. Backend-agnostic render-graph contracts remain outside the Vulkan folder.
