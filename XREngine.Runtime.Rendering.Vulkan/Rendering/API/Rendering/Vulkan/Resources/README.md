# Vulkan Resources

Owns backend resource allocation, physical image/buffer lifetime, staging and upload services, swapchain image/framebuffer resources, and resource registration helpers. Engine resource wrappers live under `BackendObjects/`.
