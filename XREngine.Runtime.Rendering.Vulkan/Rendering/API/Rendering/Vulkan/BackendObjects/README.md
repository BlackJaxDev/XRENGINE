# Vulkan BackendObjects

Owns Vulkan API wrappers around engine resources such as buffers, textures, framebuffers, materials, mesh renderers, programs, queries, and samplers. Small enums and interop structs belong in `Types/`.
