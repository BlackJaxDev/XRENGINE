# Vulkan Commands

Owns command-buffer allocation, recording, command-chain scheduling, blits, readbacks, indirect draw, render-state application, dirty tracking, and one-time submit helpers. Resource planning and wrapper objects live elsewhere.
