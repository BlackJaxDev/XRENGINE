# Advanced admission and profile audit — 2026-09-07

Source inventory for ARP-A01 on `8b104bf7a` plus current working changes.
This closes the path/contract audit, not ARP-V02 or production acceptance.

| Boundary | Source and executable contract | Observable state |
|---|---|---|
| Policy | `AdvancedRenderPipelineSelectionResolver.Resolve`: Disabled selects LegacyDefault without evaluation; Diagnostic evaluates and selects LegacyDefault; Available selects Advanced when generically supported and LegacyDefault otherwise; Required selects Advanced or None with failure. Unknown modes throw. | `RuntimeEngine.Rendering.Stats.RendererState` records mode, effective kind, capability evaluation/support and rejection; `EngineProfilerDataSource` carries these to the editor render profiler. These are last-renderer selection statistics, not a per-output acceptance certificate. |
| Generic features | `AdvancedRenderPipelineCapabilityResolver.Resolve` requires a renderer, recognized backend, integer targets/visibility encoding, compute, storage buffers, indirect submission, texture indirection, synchronization, frame slots and VisibilityBuffer shader family. Stereo additionally requires array resources; OpenGL stereo requires executable multiview raster. The first missing requirement wins. | MCP `get_advanced_profile_diagnostics` and `get_render_state.advancedProfile.profile.genericCapabilityResolver` expose support, rejection and explanation separately from the backend. |
| OpenGL executor | `OpenGLRenderer.AdvancedVisibilityExecution.GetAdvancedVisibilityFamilyAdmission` requires indirect-count draws, bindless handles, 90 SSBO bindings, 15 compute/8 vertex/2 fragment storage blocks, 3 UBO bindings, 6 image units and 5 compute texture units. Runtime and shader readiness must succeed. A missing current context is PendingResources until prior readiness exists. Reservation uses the bounded output registry. | `admissionState/reason`, `bindingState`, `failureReason`, `reservationCurrent` and output-reservation diagnostics. Generic texture-array/ordinary-indirect capability fallbacks do not override these stricter executable requirements. |
| OpenGL stages/views | `SupportsAdvancedVisibilityStage` names preparation, visibility raster, depth pyramid/late visibility, AO, classification and native shading. Enqueue requires a current exact reservation and canonical contiguous layers, then checks stage order/publication/frame/view identity. `AdvancedStereo` checks the actual OVR programs. | `stages` and `perEye` identify the output/generation; authored stage rows explicitly do not certify GPU completion. |
| Vulkan executor | `VulkanCommandRuntime.AdvancedPipelineCapabilities` requires an operational device, indirect-count draw, ready scene/visibility resources and required programs for admission. Reservation additionally needs a nonzero output, active storage generation, bounded output bank/workspace and current exact reservation. | The same structured admission/binding fields plus Vulkan bank diagnostics. The global feature snapshot intentionally retains shader family None until selection has an output identity; a generic MissingShaderFamily result can coexist with a bound admitted output. |
| Selection to reservation | `EngineRenderingSettingsApplication.Preferences.ResolveAdvancedRenderPipelineSelection` reserves only admitted desktop, XR-eye or explicitly intended offscreen outputs. It promotes the effective shader-family snapshot for a successful reservation. PendingResources may retain the configured Advanced source while execution remains pending. Required failures throw `AdvancedRenderPipelineNotSupportedException`; Available fallback or retained-unbound profiles log their reason. | Last selection statistics and the selected output's separate binding state. Retaining an Advanced asset does not imply execution. |
| Execution versus acceptance | `AdvancedProductionCutoverContract.EvaluateStatus` requires provider compatibility, admitted backend, bound output and a current reservation. Success still returns runtime validation Pending and production acceptance Pending. Unbound supported profiles are PendingResources. `AdvancedRenderPipeline.IsProductionReady` reads production acceptance. | MCP `profile.cutover` and `executionAdmitted/executionBlocker`; there is no unconditional executable production certification. A misleading XML summary was corrected. |
| Providers | Full shading admits GI None or the native probe/IBL provider. Other GI modes/providers are rejected as missing, mismatched or not integrated. AO permits null or supported `AdvancedDepthGtaoProvider`; other providers are rejected. Minimal visibility output bypasses providers for stages it omits. | Provider blockers flow into cutover/admission output. Major new GI integrations remain outside this task. |
| Stereo topology | `AdvancedRenderPipeline.StereoAndViews` maps the XR two-pass stage family to RvcTwoPass; non-stereo to Mono; OpenGL stereo to OpenGlSinglePassStereo; Vulkan stereo to VulkanMultiview. Vulkan array support derives from enabled multiview. | `profile.stereoMode`, offscreen profile and per-eye/layer records. Actual headset/mode parity remains V35/V44/V45 and XR rows. |
| Offscreen/stage omissions | Minimal visibility profiles include FrameBegin, Deformation, VisibilityPreparation, VisibilityRaster, DepthPyramidAndLateVisibility and Output, with visibility/depth/capture preparation. Full profiles include the remaining native stages. Offscreen flags gate late transparency, post, temporal history, bloom/DoF. Screen-space UI is desktop-present only without an offscreen profile. Post-AA has its own predicate and is not equivalent to the general post flag. | Offscreen profile plus observed stage/resource inventory; actual mirror, portal, probe and thumbnail output still requires the respective runtime boxes. |

MCP's bound-output fields were also observed during the OpenGL stereo runs:
Admitted/Bound/current reservation, distinct eye identities, and runtime/
production acceptance Pending. This observation corroborates reporting only;
it does not execute every negative admission case or all stage families.

Evidence extraction used broker run `4ed38e9892694b9cbb49cecd41e4918d`
(requested/actual `gpt-5.6-sol`, low effort). The coordinator additionally
traced the actual settings-selection/reservation boundary, OpenGL admission
implementation and editor profiler consumer, which were outside that packet.
Remaining work belongs to V02, the per-feature V rows and the already tracked
implementation defects; no duplicate acceptance checklist is introduced.
